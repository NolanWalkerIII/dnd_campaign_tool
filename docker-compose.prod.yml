services:
  dnd:
    build: .
    volumes:
      - ./instance:/app/instance
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=root_default"
      - "traefik.http.routers.campaigncodex.rule=Host(`campaigncodex.momentumlabs.cloud`)"
      - "traefik.http.routers.campaigncodex.entrypoints=web,websecure"
      - "traefik.http.routers.campaigncodex.tls=true"
      - "traefik.http.routers.campaigncodex.tls.certresolver=mytlschallenge"
      - "traefik.http.services.campaigncodex.loadbalancer.server.port=5000"
    networks:
      - root_default

networks:
  root_default:
    external: true
